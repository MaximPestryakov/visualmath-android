dependencies:
  pre:
    - yes | android update sdk --no-ui --all --filter android-25
    - yes | android update sdk --no-ui --all --filter build-tools-25.0.2
    - yes | android update sdk --no-ui --all --filter extra-android-m2repository
    - yes | android update sdk --no-ui --all --filter platform-tools
    - yes | android update sdk --no-ui --all --filter tools

test:
  override:
    - ./gradlew assembleDebug
