<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.visualmath.android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".lectureboard.LectureBoardActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login.LoginActivity" />
        <activity android:name=".lecture.LectureActivity" />
        <activity android:name=".signup.SignUpActivity" />
        <activity
            android:name=".synclecture.SyncLectureActivity"
            android:theme="@style/AppTheme.NoActionBar" />

        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="4bc8816d05de7b025fc46d1668c1d4cc69e3761a" />

        <activity android:label="@string/app_name"
            android:name="com.example.android.paint.PaintActivity"
            android:noHistory="true"/>

    </application>

</manifest>
